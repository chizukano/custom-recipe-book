<div class="bg-white m-3 p-3 h1">
  <h1>Categories</h1>
  <div class="row">
    <% @categories.each do |category| %>
      <%= render "categories/index_card", category: category %>
    <% end %>
  </div>
</div>
<div class="bg-white m-3 p-3">
  <div class="col-6">
    <h2 class="h4">Add a new food category</h2>
    <%# <%= simple_form_for [@category] do |f| %>
      <%# <%= f.input :name, placeholder: "Food Category", label: false %>
      <%# <%= f.input :photo, label: false %> %>
      <%# <%= f.submit "Add to the list", class: "btn btn-info" %> %> %>
    <%# <% end %>
  </div>
</div>
<% if user_signed_in? %>
  <div class="bg-white m-3 p-3">
    <div class="col-6">
      <%= link_to "Your Cookbook page", user_path(current_user), class: "btn btn-info" %>
    </div>
  </div>
<% end %>
